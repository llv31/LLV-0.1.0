<?php
$this->headTitle(_('HEAD_TITLE_PARTNERS') . ' | ');
/**  */
$this->headLink()->appendStylesheet($this->buildDesignUrl('partners/css/index.css'));
$this->headScript()->appendFile($this->buildDesignUrl('partners/js/index.js'));
$page = $this->page;
?>

<div class="page">
<div class="partners">
    <h1>
        <?= $page->title; ?>
    </h1>
    <?= $page->content; ?>
</div>
</div>
